import { AbstractRendererPlugin } from '@antv/g-lite';
import type { GesturePluginOptions } from './interfaces';
export declare class Plugin extends AbstractRendererPlugin {
    private options;
    name: string;
    constructor(options?: Partial<GesturePluginOptions>);
    init(): void;
    destroy(): void;
}
