import { __assign } from "tslib";
import Children from './children';
export function generateFrameElement(cur, element) {
  if (!element) return;
  return Children.map(element, function (child) {
    var key = child.key,
      props = child.props;
    var newProps = cur[key] ? cur[key].to : {};
    var children = generateFrameElement(cur, props.children);
    return Children.cloneElement(child, __assign(__assign({}, newProps), {
      children: children
    }));
  });
}